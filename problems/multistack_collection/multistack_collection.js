/*
  Given a size constraint, create a collection that utilizes Stacks to 
  store elements. Once a stack has reached the size constraint, another Stack
  will be created and elements will be placed in the subsquent Stack.

*/

(function(CSC){

CSC.multiStackCollection = function(stackSize){
  
};

CSC.multiStackCollection.prototype.push = function(value){
  
};

CSC.multiStackCollection.prototype.pop = function(){
  
};

CSC.multiStackCollection.prototype.popAt = function(stackIndex){
  
};

})(CSC);
